COMMENT ON TABLE tb_categorias IS
  'Tabela responsável por armazenar as categorias utilizadas para classificar os produtos do catálogo.';

COMMENT ON COLUMN tb_categorias.id IS
  'Identificador único da categoria.';

COMMENT ON COLUMN tb_categorias.nome IS
  'Nome da categoria utilizada para classificação dos produtos.';
