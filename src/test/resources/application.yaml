spring:
  datasource:
    url: jdbc:h2:mem:testdocinhos
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

  web:
    cors:
      allowed-origins: test-web-api

  security:
    jwt:
      secret: u4uLv6jiw9nqZbx4a8pp57VfO/+Winj9IV8EMN8LBBQ=
      expiration: 3600000

  flyway:
    enabled: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
